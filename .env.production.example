# ==========================================
# Cleanup Tracker - Production Environment
# ==========================================
# IMPORTANT: Copy this file to .env.production and fill in your actual values
# NEVER commit .env.production to version control

# Node Environment
NODE_ENV=production

# Server Port
PORT=5051

# ==========================================
# MongoDB Configuration
# ==========================================
# For production, use MongoDB Atlas (free tier available)
# Format: mongodb+srv://username:password@cluster.mongodb.net/cleanup-tracker
MONGO_URI=mongodb://mongodb:27017/cleanup-tracker

# ==========================================
# JWT Authentication Secrets
# ==========================================
# CRITICAL: Generate secure random secrets for production
# Use: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
# NEVER use the same secrets across environments

JWT_SECRET=GENERATE_YOUR_OWN_SECRET_HERE
JWT_ACCESS_SECRET=GENERATE_YOUR_OWN_SECRET_HERE
JWT_REFRESH_SECRET=GENERATE_YOUR_OWN_SECRET_HERE

# JWT Token Expiration
JWT_ACCESS_EXPIRATION=15m
JWT_REFRESH_EXPIRATION=7d

# ==========================================
# CORS & Frontend Configuration
# ==========================================
# Comma-separated list of allowed frontend URLs
FRONTEND_URL=https://yourdomain.com,https://www.yourdomain.com

# ==========================================
# Google Sheets Integration (Optional)
# ==========================================
# Published Google Sheet CSV URL for inventory sync
# Instructions: File > Share > Publish to web > CSV
INVENTORY_CSV_URL=https://docs.google.com/spreadsheets/d/e/YOUR-SHEET-ID/pub?output=csv

# ==========================================
# Rate Limiting
# ==========================================
# General API rate limit (requests per 15 minutes)
RATE_LIMIT_MAX=100

# Authentication endpoints rate limit (stricter)
AUTH_RATE_LIMIT_MAX=5

# ==========================================
# Upload & Performance
# ==========================================
# Maximum request body size
UPLOAD_LIMIT=10mb

# API timeout (milliseconds)
API_TIMEOUT=30000

# ==========================================
# Security Headers (Optional Overrides)
# ==========================================
# Uncomment to customize security settings
# HELMET_CSP_ENABLED=true
# ALLOWED_ORIGINS=https://yourdomain.com

# ==========================================
# Monitoring & Logging (Optional)
# ==========================================
# Sentry DSN for error tracking
# SENTRY_DSN=https://your-sentry-dsn@sentry.io/project-id

# Log level: error, warn, info, debug
# LOG_LEVEL=info
